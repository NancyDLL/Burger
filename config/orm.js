var connection = require('./connection.js');

//PSUEDOCODE
//This is where I would add the functions to handle the queries:
//A function to select from the DB table
//A function to insert bergers into the DB table
//A function to update the DB table

module.exports = orm;