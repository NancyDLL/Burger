var orm = require("../config/orm.js");

//PSUEDOCODE
//This is where the function for calling the ORM
//
//orm.selectAll("burgers", function(res)
//cb(res);
//orm.insertOne("burgers", cols, vals, function(res)
//cb(res);
//orm.insertOne("burgers", cols, vals, function(res)
//cb(res);

module.exports = burger;
